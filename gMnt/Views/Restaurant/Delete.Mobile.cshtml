@model gLibrary.Models.Restaurant

@{
    ViewBag.Title = "Restaurant - Delete";
}

<h2>Delete Restaurant</h2>

<h3 class="delete-title">Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Restaurant</legend>

    <div class="line">
        <div class="line-left">@Html.LabelFor(model => model.Id)</div>
        <div class="line-right">
            @Html.DisplayFor(model => model.Id)
        </div>
    </div>
    <div class="line">
        <div class="line-left">@Html.LabelFor(model => model.Bilingual.Name)</div>
        <div class="line-right">
            @Html.DisplayFor(model => model.Bilingual.Name)
        </div>
    </div>
    <div class="line">
        <div class="line-left">@Html.LabelFor(model => model.Bilingual.AlienName)</div>
        <div class="line-right">
            @Html.DisplayFor(model => model.Bilingual.AlienName)
        </div>
    </div>
</fieldset>

@using (Html.BeginForm()) {
    <p class="delete-title">
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
